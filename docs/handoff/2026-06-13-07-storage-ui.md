# 07 — Storage & Backup screen + wiring

**Date:** 2026-06-13
**Status:** ✅ Code done (typecheck passes) — not yet run on device

## What was done
User-facing surface for Sprint 3: a Storage & Backup screen reachable from the Stats
tab.

- `src/services/storage.ts` — `getStorageStats(sqlite)` (video/thumb bytes via
  `SUM(size_bytes)`, video count, missing count) + `formatBytes()`.
- `app/storage.tsx` — new screen:
  - Storage usage card + amber warning when assets are `missing`.
  - "Clean up orphaned files" → `mediaRepo.cleanupOrphans()` with result alert.
  - "Export…" → `exportBackup` + `shareBackup`.
  - "Import…" → pick → prepare → conflict Alert (Cancel / Merge / Replace all). Replace
    requires a second destructive confirmation. Integrity warnings surfaced in the
    dialog. Result summary alert.
  - "Private by design" footer note.
- `app/_layout.tsx` — registered `<Stack.Screen name="storage">` (card presentation).
- `app/(tabs)/stats.tsx` — added a "Storage & Backup" card linking to `/storage`.

## Files touched
- `src/services/storage.ts` — **new**
- `app/storage.tsx` — **new**
- `app/_layout.tsx` — Stack.Screen entry
- `app/(tabs)/stats.tsx` — entry-point card + imports

## Dependencies
- Consumes services from handoff 05 + 06. No new packages.

## Blockers
- Native rebuild required (export/import use the zip native module) — handoff 04.

## Manual actions required
- None for code. After native rebuild, smoke-test the four actions on the screen.

## Notes / follow-ups
- `/storage` push is cast `as Href` because expo typed-routes hadn't regenerated at
  build time. The cast becomes unnecessary after the first `expo start` post-rebuild;
  can be removed then.
- Verification done this session: `npx tsc --noEmit` → EXIT=0 across all Sprint 3
  files. No device/emulator run (native rebuild pending on the user's machine).
