# 10 — Onboarding flow

**Date:** 2026-06-13
**Status:** ✅ Done — `tsc --noEmit` clean.
**Sprint:** 4 (Stats + Polish)

## What was done
- 3-slide intro (paged horizontal `ScrollView`, `pagingEnabled`), dots indicator, Skip + Next/Get started.
- Gated by MMKV `onboarding_completed` (`preferences.isOnboardingComplete` / `setOnboardingComplete`, already present).
- First launch redirects to `/onboarding` from `AppStartup` effect; finishing or skipping sets the flag and `router.replace('/(tabs)')`.
- Copy fully i18n'd (`onboarding.*` keys, EN + PT-BR).

## Files touched
- `app/onboarding.tsx` — **new**. Slides defined `as const` so `t(titleKey)` keeps literal key types.
- `app/_layout.tsx` — registered `onboarding` screen (`headerShown: false`); `AppStartup` redirects when onboarding incomplete.

## Decisions / gotchas
- `router.replace('/onboarding' as Href)` / `'/(tabs)' as Href` — typed-routes file (`.expo/types`) is stale until the dev server regenerates it; cast avoids a false `tsc` error. Remove casts after first `expo start` if desired.
- Slide width from `Dimensions.get('window')`.

## Manual actions required
- To re-test onboarding after it's been dismissed: clear app data, or temporarily call `preferences` to reset the flag. (No in-app reset button shipped.)

## Follow-ups
- Optional: "Reset onboarding" debug action in Settings for beta testers.
